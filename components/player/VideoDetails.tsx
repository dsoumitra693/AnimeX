import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const VideoDetails = () => {
    return (
        <View>
            <Text style={{color:'white'}}>VideoDetails</Text>
        </View>
    )
}

export default VideoDetails

const styles = StyleSheet.create({})